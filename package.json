{
  "name": "test_frontieres",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "python -m http.server 8000 --directory public --bind 0.0.0.0",
    "start": "npm run build && npm run serve",
    "dev": "tsc --watch & python -m http.server 8000 --directory public --bind 0.0.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
